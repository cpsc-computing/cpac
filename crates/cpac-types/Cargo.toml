[package]
name = "cpac-types"
description = "Shared types and error definitions for the CPAC engine"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
thiserror = { workspace = true }
