[package]
name = "cpac-predict"
description = "Predictive modeling for CPAC: delta, multi-delta, and context predictors"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
cpac-types = { workspace = true }
